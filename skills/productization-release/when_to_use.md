# When to use

- When a milestone, sprint, or feature set is complete and needs to be deployed.
- Before cutting a new release branch or Git tag.
- When configuring CI/CD pipelines (GitHub Actions, GitLab CI, etc.).
- When containerizing the application (Docker) for production hosting.
